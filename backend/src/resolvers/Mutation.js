const Mutation = {
  /**
   * Add a task
   */
  createRestaurant: async (parent, { title, tags, link}, { db, pubSub }) => {
    
  },
  
  createTag: async (parent, { type, name }, { db, pubSub }) => {
    
  },
  
};

export default Mutation;
